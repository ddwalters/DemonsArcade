using UnityEngine;

public class PlayerStats : MonoBehaviour
{
    int currentLvl;
    int currentXP;
    int nextLevelXP;
    // xp slider (should have tag)
    // lvl text

    int maxHealth;
    int currentHealth;
    // health text
    // hud health icon (should have tag)

    int maxStamina;
    int currentStamina;
    // stamina text
    // hud stamina slider (should have tag)

    int maxMana;
    int currentMana;
    // mana text
    // hud mana slider (should have tag)

    private void Start()
    {
        // get xp slider by tag
        // get health slider by tag
        // get stamina slider by tag
        // get mana slider by tag
    }
}
